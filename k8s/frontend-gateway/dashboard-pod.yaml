apiVersion: v1
kind: Pod
metadata:
  name: dashboard-vue
  labels:
    app: dashboard
spec:
  containers:
  - name: vue-app
    image: meteo/meteo-dashboard:v1
    imagePullPolicy: Never
    ports:
    - containerPort: 80